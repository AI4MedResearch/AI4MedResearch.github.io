<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ai4medresearch.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ai4medresearch.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-30T00:34:29+00:00</updated><id>https://ai4medresearch.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">On-Device Medical Intelligence - Converging MedGemma 1.5 4B and LiteRT</title><link href="https://ai4medresearch.github.io/blog/2026/medgemma1-5/" rel="alternate" type="text/html" title="On-Device Medical Intelligence - Converging MedGemma 1.5 4B and LiteRT"/><published>2026-03-28T00:00:00+00:00</published><updated>2026-03-28T00:00:00+00:00</updated><id>https://ai4medresearch.github.io/blog/2026/medgemma1-5</id><content type="html" xml:base="https://ai4medresearch.github.io/blog/2026/medgemma1-5/"><![CDATA[<p>In the rapidly evolving landscape of healthcare AI, the transition from massive, cloud-dependent models to specialized, on-device intelligence is not just a trend—it’s a clinical necessity. Medical data is inherently sensitive, and the requirements for privacy (HIPAA compliance), zero-latency reasoning, and offline accessibility in remote or high-security environments are paramount.</p> <p>Here we are diving into how to bring state-of-the-art medical multimodal intelligence directly to the edge. By converting <strong>MedGemma 1.5 4B</strong> to the specialized <strong><a href="https://github.com/google-ai-edge/LiteRT-LM">LiteRT</a></strong> (<code class="language-plaintext highlighter-rouge">.litertlm</code>) format, we unlock the ability to perform complex clinical analysis—including MRI interpretation and EHR questioning—entirely within a local web browser using WebGPU.</p> <h2 id="1-the-rise-of-on-device-medical-intelligence">1. The Rise of On-Device Medical Intelligence</h2> <p>Traditional medical AI often relies on sending high-resolution scans and patient records to powerful GPU clusters in the cloud. While effective, this approach introduces significant bottlenecks:</p> <ul> <li><strong>Privacy Risks</strong>: Every byte of data leaving the hospital network is a potential point of failure.</li> <li><strong>Latency</strong>: In critical care, waiting for a round-trip to a data center can be the difference between a prompt diagnosis and a delayed one.</li> <li><strong>Connectivity</strong>: Many clinical environments (ORs, remote clinics, or mobile health units) suffer from inconsistent internet access.</li> </ul> <p>On-device intelligence solves these by performing inference where the data is born. With the release of Google’s <strong>Gemma 3</strong> architecture and its medical sibling <strong>MedGemma 1.5</strong>, the “edge” is now powerful enough to handle 4-billion parameter multimodal models.</p> <h2 id="2-medgemma-15-4b-a-multimodal-leap">2. MedGemma 1.5 4B: A Multimodal Leap</h2> <p><strong>MedGemma 1.5 4B</strong> represents a significant architectural shift over its predecessors. While MedGemma 1.0 was a pioneer in clinical text understanding, the 1.5 iteration—built on the <strong>Gemma 3</strong> foundation—is a true multimodal powerhouse.</p> <h3 id="key-advancements">Key Advancements:</h3> <ul> <li><strong>From 2D to 3D</strong>: Previous models focused on 2D images like X-rays or dermoscopy. MedGemma 1.5 natively interprets <strong>3D medical volumes</strong> from CT and MRI scans.</li> <li><strong>Longitudinal Reasoning</strong>: One of the model’s strongest features is its ability to track disease progression by comparing historical scans against current ones.</li> <li><strong>Clinical Accuracy</strong>: EHR Question Answering accuracy has jumped from ~68% in version 1 to a staggering <strong>~90%</strong> in version 1.5.</li> <li><strong>Anatomy Localization</strong>: Precise identification of anatomical structures and abnormalities saw an improvement from ~3% IoU to <strong>~38% IoU</strong>.</li> </ul> <table> <thead> <tr> <th style="text-align: left">Capability</th> <th style="text-align: left">MedGemma 1 (4B)</th> <th style="text-align: left">MedGemma 1.5 (4B)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>Base Architecture</strong></td> <td style="text-align: left">Gemma 2</td> <td style="text-align: left"><strong>Gemma 3</strong></td> </tr> <tr> <td style="text-align: left"><strong>Imaging Support</strong></td> <td style="text-align: left">2D Focus (X-rays)</td> <td style="text-align: left"><strong>3D (CT/MRI) &amp; WSIs</strong></td> </tr> <tr> <td style="text-align: left"><strong>Temporal Reasoning</strong></td> <td style="text-align: left">Single-scan</td> <td style="text-align: left"><strong>Longitudinal Tracking</strong></td> </tr> <tr> <td style="text-align: left"><strong>EHR QA Accuracy</strong></td> <td style="text-align: left">~68%</td> <td style="text-align: left"><strong>~90%</strong></td> </tr> </tbody> </table> <h2 id="3-deep-dive-into-the-litertlm-format">3. Deep Dive into the .litertlm Format</h2> <p>To run MedGemma efficiently on the edge, we leverage the <strong>.litertlm</strong> format. This isn’t just another file extension; it is LiteRT’s (formerly TensorFlow Lite) specialized bundle for Generative AI.</p> <h3 id="why-litertlm-for-medgemma">Why .litertlm for MedGemma?</h3> <ol> <li><strong>Stateful Optimization</strong>: Unlike standard <code class="language-plaintext highlighter-rouge">.tflite</code> graphs, a <code class="language-plaintext highlighter-rouge">.litertlm</code> bundle is designed for the iterative nature of LLMs. It contains separate, optimized graphs for <strong>Prefill</strong> (processing the prompt) and <strong>Decode</strong> (generating tokens one-by-one), while natively managing the <strong>KV-cache</strong>.</li> <li><strong>Multimodal Synergy</strong>: MedGemma requires a vision encoder and a language head to work in tandem. <code class="language-plaintext highlighter-rouge">.litertlm</code> bundles these disparate components into a single, self-describing artifact, ensuring the vision-language projection layers are always synchronized.</li> <li><strong>Hardware Native</strong>: The format is built to leverage the <strong>LiteRT GenAI API</strong>, which provides highly optimized kernels for mobile GPUs and NPUs, significantly outperforming generic graph execution.</li> </ol> <h2 id="4-the-conversion-workflow">4. The Conversion Workflow</h2> <p>The transformation from raw PyTorch weights to a production-ready LiteRT bundle is an intricate process handled via the <a href="https://github.com/google-ai-edge/litert-torch"><code class="language-plaintext highlighter-rouge">litert_torch</code></a> export pipeline. It starts with the original <a href="https://huggingface.co/google/medgemma-1.5-4b-it">MedGemma 1.5 4B IT</a> weights from Google and re-architects them for high-performance edge execution.</p> <h3 id="the-logic-behind-the-conversion">The Logic Behind the Conversion</h3> <p>Instead of a simple “file format save,” the conversion logic performs several critical architectural bridges and optimizations:</p> <ol> <li><strong>Structural Alignment (Architecture Bridging)</strong>: MedGemma 1.5 is built on the Gemma 3 architecture. In some versions of the Hugging Face <code class="language-plaintext highlighter-rouge">transformers</code> library, the vision modules (tower and projector) are nested deeply within the model structure. Our workflow includes a <strong>structural patch</strong> that maps these nested components to the top level of the model class. This ensures the export engine can accurately “see” and trace the multimodal connection points during the graph-generation phase.</li> <li><strong>Multimodal Graph Tracing</strong>: The conversion initiates a <code class="language-plaintext highlighter-rouge">image_text_to_text</code> export task. This process traces the mathematical flow of data through both the vision encoder and the language head. It effectively captures how an MRI image is transformed into tokens and how those tokens are processed by the LLM to generate a clinical description.</li> <li><strong>Prefill Bucketing</strong>: To optimize the “time-to-first-token” on edge devices, the workflow generates specialized graphs for different <strong>prefill lengths</strong> (e.g., 128, 256, 512 tokens). This allows the runtime to use the most efficient computation path based on the size of the user’s initial prompt or image metadata.</li> <li><strong>Specialized Dual-Quantization</strong>: To compress the 4B parameter model to a browser-friendly ~3GB, we apply distinct quantization strategies to different components: <ul> <li><strong>LLM Core</strong>: Uses a <code class="language-plaintext highlighter-rouge">dynamic_wi8_afp32</code> recipe (8-bit weights with 32-bit activations), balancing reasoning depth with memory footprint.</li> <li><strong>Vision Encoder</strong>: Uses a <code class="language-plaintext highlighter-rouge">weight_only_wi8_afp32</code> recipe, ensuring that the high-dimensional features required for medical imaging are preserved while still reducing the storage overhead.</li> </ul> </li> <li><strong>KV-Cache Architecture</strong>: The workflow configures a fixed-length <strong>Key-Value (KV) cache</strong> (typically 4096 tokens). This is embedded into the LiteRT graph, enabling the model to “remember” the context of a long medical conversation without re-processing the entire history for every new word generated.</li> <li><strong>Unified Bundling</strong>: The final step packages the optimized graphs, the token embedder, the tokenizer configuration, and essential model metadata into a single, self-describing <strong>.litertlm</strong> container. This eliminates the need for external configuration files and ensures the model is “plug-and-play” for the edge runtime.</li> </ol> <h2 id="5-model-deployment-on-edge-web-browsers">5. Model Deployment on Edge Web Browsers</h2> <p>Deploying a 4-billion parameter multimodal model like MedGemma in a browser tab is a feat of modern web engineering. It transforms the browser from a simple document viewer into a secure, hardware-accelerated sandbox for private clinical intelligence. To verify the effectiveness of the converted model, we implement a simple GUI prototype written in JavaScript/TypeScript that runs on a local web browser, in which the model deployment is optimized through a multi-layered architectural approach.</p> <video controls="" width="100%"> <source src="/assets/video/medgemma_edge_demo.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> <h3 id="hardware-first-verification">Hardware-First Verification</h3> <p>The deployment begins with an environmental handshake. Before attempting to load any model, the application verifies the presence of the <strong>WebGPU API</strong> (<code class="language-plaintext highlighter-rouge">navigator.gpu</code>). This is the application’s “gatekeeper”—without native GPU access, the computational overhead of a 4B parameter model would be too high for a standard browser thread.</p> <h3 id="dynamic-runtime-resolution-wasm">Dynamic Runtime Resolution (WASM)</h3> <p>Once hardware is confirmed, the application initializes the <strong>LiteRT GenAI runtime</strong>. Instead of shipping massive binary loaders with the app, we utilize the <code class="language-plaintext highlighter-rouge">FilesetResolver</code> to pull specialized WebAssembly (WASM) runtimes (like <code class="language-plaintext highlighter-rouge">genai_wasm_internal.js</code>) from a high-performance CDN. This ensures the edge engine is always running the latest version compatible with the MedGemma 1.5 bundle format.</p> <h3 id="local-asset-setup">Local Asset Setup</h3> <p>For the GUI prototype to function correctly, the <code class="language-plaintext highlighter-rouge">.litertlm</code> model files must be hosted locally within the application’s structure. Specifically, the model bundle should be located at the <code class="language-plaintext highlighter-rouge">public/models</code> directory. This allows the LiteRT GenAI runtime to fetch the multi-gigabyte binary artifacts directly from the same origin, bypassing complex cross-origin resource sharing (CORS) issues while maintaining high-speed local data transfer.</p> <h3 id="two-stage-fallback-strategy">Two-Stage Fallback Strategy</h3> <p>To maximize clinical accessibility, we implemented a robust <strong>Primary-to-Backup loading loop</strong>:</p> <ul> <li><strong>Primary Attempt (MedGemma 1.5 4B)</strong>: The system first tries to allocate resources for the high-fidelity medical model. This model provides the deep clinical reasoning required for complex MRI/CT analysis.</li> <li><strong>Automatic Fallback (Gemma 3 2B)</strong>: If the primary load fails—common on devices with less than 16GB of RAM or limited VRAM—the logic catches the error and immediately attempts to initialize the lighter Gemma 3 engine. This ensures the clinician is never left without an active intelligence layer.</li> </ul> <p>In the future, it would be more handful to have a robust MedGemma with less than 2B.</p> <h3 id="integrity--cache-management">Integrity &amp; Cache Management</h3> <p>Handling 3GB model files at the edge introduces “stale cache” risks. If a browser attempts to load a partially-downloaded or outdated model file, initialization will fail. Our deployment employs a <strong>Cache-Busting Strategy</strong>, appending a dynamic timestamp query parameter (<code class="language-plaintext highlighter-rouge">?v=${Date.now()}</code>) to the model asset path. This forces the browser to verify the file’s integrity and ensures that the clinical engine is always synchronized with the correct <code class="language-plaintext highlighter-rouge">.litertlm</code> artifact.</p> <h3 id="multimodal-inference-pipeline">Multimodal Inference Pipeline</h3> <p>The inference process is not just a text loop; it is a coordinated orchestration of vision and language:</p> <ul> <li><strong>Image Ingestion</strong>: Medical scans are loaded into browser memory as <code class="language-plaintext highlighter-rouge">HTMLImageElement</code> objects.</li> <li><strong>Part-Based Payload</strong>: We construct a multimodal payload consisting of the image source followed by a structured text prompt.</li> <li><strong>Smart Streaming</strong>: To provide a fluid “typing” experience, the <code class="language-plaintext highlighter-rouge">generateResponse</code> method uses a callback that implements a <strong>Smart Accumulator</strong>. This logic detects whether the backend is sending cumulative strings or incremental tokens, ensuring that the response flows smoothly without flickering, repeating, or vanishing.</li> </ul> <h3 id="secure-execution-environment">Secure Execution Environment</h3> <p>To enable the advanced memory features (like <code class="language-plaintext highlighter-rouge">SharedArrayBuffer</code>) required for GPU-accelerated inference, the deployment requires a “Secure Context.” This is enforced via mandatory HTTP security headers:</p> <ul> <li><strong>COOP (Cross-Origin Opener Policy)</strong>: <code class="language-plaintext highlighter-rouge">same-origin</code></li> <li><strong>COEP (Cross-Origin Embedder Policy)</strong>: <code class="language-plaintext highlighter-rouge">require-corp</code></li> </ul> <p>This architecture allows expert-level medical AI to run with 100% privacy, utilizing the power already sitting on the clinician’s desk.</p> <h2 id="6-conclusion-and-future-work">6. Conclusion and Future Work</h2> <p>The integration of MedGemma 1.5 4B with LiteRT represents a significant milestone in medical AI accessibility, delivering expert-level multimodal intelligence directly to the browser. By enabling private, always-available assistants on-device, we are bridging the gap between cloud-scale performance and edge efficiency, ensuring clinicians can access critical decision support regardless of their connectivity.</p> <p>To transition this from a research prototype to a global production tool for resource-constrained environments, our future roadmap focuses on three key optimizations:</p> <ul> <li> <p><strong>Int4 QAT Quantization</strong>: Implementing Quantization-Aware Training to drastically reduce memory requirements for entry-level mobile and laptop hardware.</p> </li> <li> <p><strong>Speculative Decoding</strong>: Integrating draft models to achieve a 2–3× increase in inference speed, essential for high-volume clinical workflows.</p> </li> <li> <p><strong>Localized Fine-Tuning</strong>: Adapting the model to regional medical terminology and local languages to ensure real-world clinical utility.</p> </li> </ul> <p>A primary example of the need for these advancements is found in Indonesia’s 3T regions (underdeveloped, frontier, and outermost). In these settings, healthcare workers require high-accuracy tools that operate fully offline, speak Bahasa Indonesia, and run on the affordable, low-spec hardware already available in community health centers (Puskesmas).</p> <p>Resources:</p> <ul> <li><strong>Model (<code class="language-plaintext highlighter-rouge">.litertlm</code>)</strong>: <a href="https://huggingface.co/ai4med-id/medgemma-1.5-4b-it-litertlm">huggingface.co/ai4med-id/medgemma-1.5-4b-it-litertlm</a></li> <li><strong>Implementation &amp; GUI Prototype</strong>: <a href="https://github.com/AI4MedResearch/edge-ai">github.com/AI4MedResearch/edge-ai</a></li> </ul>]]></content><author><name>Muhammad Ghifary</name></author><category term="distill"/><category term="formatting"/><summary type="html"><![CDATA[converting MedGemma 1.5 4B to LiteRT-LM and running it on edge browser]]></summary></entry></feed>